# Configuration for PRODUCTION SERVER (Ubuntu/Debian EC2)
# This file is copied to /etc/security-updater/config.env by install.sh

# Paths — standard Linux locations
LOG_DIR=/var/log/security-updates
REPORT_DIR=/var/reports/security-updates

# Server Information — CHANGE THESE
SERVER_NAME=EC2-Production-Server
ENVIRONMENT=production

# Telegram Notifications (optional — leave empty to disable)
TELEGRAM_BOT_TOKEN=your-bot-token-here
TELEGRAM_CHAT_ID=your-channel-id-here
